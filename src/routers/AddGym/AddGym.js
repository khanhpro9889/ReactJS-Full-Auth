import React from 'react';
import PropTypes from 'prop-types';

const AddGym = props => {
    return (
        <div>
            
        </div>
    );
};

AddGym.propTypes = {
    
};

export default AddGym;