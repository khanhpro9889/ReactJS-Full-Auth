import React from 'react';
import PropTypes from 'prop-types';

const Ranking = props => {
    return (
        <div>
            
        </div>
    );
};

Ranking.propTypes = {
    
};

export default Ranking;