import React from 'react';
import { Wrapper } from '../Wrapper';
import { Boxed } from '../Boxed';

const Header = () => {
    return (
        <Wrapper>
            <Boxed>
                
            </Boxed>
        </Wrapper>
    )
}

export default Header;